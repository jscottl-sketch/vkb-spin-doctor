
Generate C# classes with `protoc --csharp_out=generated JoystickProfile.proto`. Add the generated `.cs` files to the solution.

---

## 3. Create an XML Schema (XSD) Matching the Protobuf Model

File `JoystickProfile.xsd` (excerpt):

