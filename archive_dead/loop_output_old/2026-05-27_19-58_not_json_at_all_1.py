*If the test suite fails, fix the underlying issue before proceeding.  Record the current coverage percentages (e.g., via `coverlet` or `dotnet test --collect:"XPlat Code Coverage"`).*

### 1.2 Create Feature Branches  
