
> The three URLs above are placeholders – replace `example` with the real owners when you actually run the commands.

### 1.4 Prepare the **oscar64** headers  

The JOYSPR sample already ships a tiny copy of the Oscar64 SDK. Copy it into the build tree:

