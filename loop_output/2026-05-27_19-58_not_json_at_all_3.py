
Commit the file on **each** feature branch (or on a separate `design` branch and merge later). This gives all contributors a single source of truth.

---

## 2. REST API – Keyboard & Joystick Injection (Phase 1)

> **Branch:** `feature/rest-input`  

### 2.1 Define the C# Payload Types (no JSON files)

Create a new folder `src/YourApp.Api/Models` and add:

